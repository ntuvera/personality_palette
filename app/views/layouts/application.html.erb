<!DOCTYPE html>
<html>
<head>

  <title>PersonalityPalette</title>
  <link href='http://fonts.googleapis.com/css?family=Hind:400,300' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body class="<%= controller_name %>">

  <div id="nav">
    <div class="container">

        <h2 class="ten columns">
          Personality Palette
        </h2>
        <ul class="six columns">
        <% if current_user %>
          <li><%= link_to "New Entry", new_user_entry_path(current_user), method: :get %></li>
          <li><%= link_to "View All", user_entries_path(current_user), method: :get %></li>
          <li><%= link_to "Logout", logout_path, method: :post %></li>
        <% else %>
            <li><%= link_to "Register", new_user_path %></li>
            <li><%= link_to "Login", :login %></li>
        <% end %>
        </ul>
    </div>
  </div>

  <div id="flash">
    <div class="container">
      <div class="sixteen columns">
        <% if current_user %>
          <p class="session-log" id="notice"><%= flash[:notice] %></p>
        <% else %>
          <p class="session-log" id="notice"><%= flash[:notice] %></p>
          <p class="session-log" id="alert"><%= flash[:alert] %></p>
        <% end %>
      </div>
    </div>
  </div>

 <%= yield %>

</body>
</html>
